---
export interface Props {
    tags: string[];
    class?: string;
}

const { tags, class: styles } = Astro.props;
---

<div class="flex flex-wrap gap-2">
    {
        tags.map((tag: string) => (
            <span class={`font-semibold ${styles}`}>{tag}</span>
        ))
    }
</div>
